{
  "name": "urbuddi-playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
  "pretest": "rimraf allure-results",
  "test:dev": "set ENV=dev&& npx playwright test --workers=2",
  "report:history": "if exist allure-report\\history (xcopy /s /i /y allure-report\\history allure-results\\history)",
  "allure:dev": "npm run pretest && (npm run test:dev || exit 0) && npm run report:history && npx allure serve allure-results"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.9",
    "allure-commandline": "^2.36.0",
    "allure-playwright": "^3.4.5",
    "rimraf": "^6.1.2"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}